// import { useState } from "react";
// import banner from 'public/favicon.png'

import { Card, CardTitle } from "../ui/card";

export const ProfileBanner = () => {
  // const [ selected, setSelected] = useState(0);

  return (
    <Card>
      <CardTitle>Banners here</CardTitle>
    </Card>
  );
};

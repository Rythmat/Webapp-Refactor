/**
 * Idea Bank — browsable reference drawer (recurring formats, the 7-slide
 * spotlight template, cross-cutting themes, content threads, activity types,
 * anthem bank). See `canonical/index.ts`.
 */
export { IDEA_BANK } from './canonical';
